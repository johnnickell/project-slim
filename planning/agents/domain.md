# Slim architecture boundary

Slim owns routes, middleware, handlers, explicit Fight Common container definitions, HTTP, presentation, and future adapters. Shared Fight packages are Composer dependencies only.
