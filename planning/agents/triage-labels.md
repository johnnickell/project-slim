# Triage states

Use only `not-planned`, `ready-for-agent`, `in-progress`, `blocked`, `in-review`, and `done`. `done` requires local acceptance, architecture and documentation checks, and `./bin/build` evidence.
